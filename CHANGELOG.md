# Changelog

## 0.2.0 - 2026-08-24

- Replaced ambiguous prescreen-safe results with an explicit no-candidate state.
- Added a real CLI, unique run manifests, JSON findings, SARIF, atomic writes, and incremental caching.
- Enforced loopback-only model access by default and fail-closed JSON Schema output validation.
- Added transactional vector database rebuilds with corpus, label schema, and embedding content hashes.
- Added Python project AST call graphs, cross-file taint propagation, and source-to-sink slices.
- Added isolated patch verification, optional explicit regression commands, and source-preserving diff export.
- Added metadata-aware hybrid RAG reranking and deterministic security benchmarks.
- Added CI, CodeQL, dependency review, dependency audit, SBOM, and release artifact workflows.
